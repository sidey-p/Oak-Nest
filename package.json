{
  "name": "oak-and-nest",
  "version": "1.0.0",
  "private": true,
  "description": "Oak & Nest - furniture e-commerce (React frontend + Express serverless API + Supabase Postgres)",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "npm install --include=dev && cd frontend && npm install --include=dev && npm run build",
    "seed": "cd backend && npm run seed"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.3.0",
    "pg": "^8.13.1"
  }
}
